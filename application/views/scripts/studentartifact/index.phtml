<script type="text/javascript">
    function getData()
    {   cv="";
        coursevalues = document.getElementsByName("course");
        for(i=0; i<coursevalues.length; i++){
            if (coursevalues[i].selectedIndex){
                cv = coursevalues[i].value;
            }
        }
        //param = "artifact=" + document.getElementsByName("title");
        //param = param + "&desc=" + document.getElementsByName("description");
        //param = param + "&course=" + cv;
        param = "course=" + cv;
        // make sure - if yes, go to process method of indicators controller
        // sending along needed data to update table, if not stay on same page
        javascript:show_confirm("studentartifact/process?" + param,
                                "studentartifact",
                                "Uploading Artifact from course " + cv);
    }
</script>
<h2>Insert New Artifact</h2>

<?php echo $this->form;?>



<h2>Artifacts</h2>
<?php echo "<table id=\"student_artifact_table\" class =\"student_artifact_table\" cellspacing=\"0\">";?>
<?php echo "<tr>";
          echo "<th>Artifact Title</th>";
          echo "<th>Memo</th>";
          echo "</tr>";
?>
<dl>
          
<?php echo $this->partialLoop('partials/artifactRow.phtml', $this->artifactinfo);?>
<?php echo "</table>" ?>
</dl>
